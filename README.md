A very simple c-header to handly my parameters for simulations I do in c. This is very, very simple and probably only useful for myself.
Compile and run the example as follows:
```console
gcc example.c
./a.out
./run_default.sh
```
